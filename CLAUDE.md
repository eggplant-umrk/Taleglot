# CLAUDE.md

このファイルは、Claude Codeがこのリポジトリで作業する際の前提知識です。作業を始める前に必ず読んでください。

## プロジェクト概要

プロダクト名は **Taleglot**。タイの実在する民話を絵本形式で読みながらタイ語を学べる、Web学習アプリ。

## ターゲット層

小学校高学年。

## コンテンツ方針

- 創作しない。実在するタイの民話を出典付きで使う
- 民話の内容が小学高学年に重すぎる場合(暴力描写など)は、物語の核(テーマ・教訓)を保ったままマイルドに調整してよい

## 技術スタック

- React + Vite
- Next.jsは使わない(理由: チームメンバーの経験値がまだ浅く、SSRやルーティングを含むNext.jsより学習コストの低いVite+Reactを選択したため)

## ディレクトリ構成のルール

- 物語データ: `src/stories/storyXX.json`
- 物語データのスキーマ: README.md「物語データの追加方法」を参照
- 画像・音声素材: `public/stories/storyXX/`
- コンポーネント: `src/components/`

## 機能仕様（2026-09-06決定）

物語データのスキーマはREADME.md「物語データの追加方法」を参照。ここでは機能面の仕様をまとめる。

### 1. 本文中の単語タップ機能

- 本文中の重要な単語だけを太字にする（全部ではない）
- 太字部分をタップすると、詳細カードが開く
- 詳細カードの中身は「暮らし」「ことば」「価値観」の3カテゴリで固定（該当情報がないカテゴリは省略可）
- カードの閉じ方（×ボタン／外側タップ／両方）は未定。実装して触ってから決めるので、どちらにも対応できる作りにしておく

### 2. 日本語・タイ語の表示/音声トグル

- 日本語・タイ語、それぞれ独立した表示オン/オフ（目のマークのイメージ）。両方オンで併記も可能
- 日本語・タイ語、それぞれ独立した音声再生/停止ボタン（表示がオンの言語について再生できる）
- ページ送りは読み上げ中でも自由に進められる（読み上げを待つ必要はない）

### 3. 振り返り機能

- クイズ形式ではなく、フラッシュカード形式にする
- 表面: 絵、裏面: 単語・意味・読み方
- 絵本を最後まで読み終えたら、自動的に振り返り（フラッシュカード）画面に遷移する（ボタンを押す必要はない）

### 4. ページ送りのアニメーション

- アニメーションなし、一瞬で切り替わる形でよい（凝った演出は不要）

### 7. 本文のコマ送り（2026-09-16決定）

- 1ページの本文は「コマ」（2〜3文程度のまとまり）単位の配列（`pages[].text` / `pages[].thai`）で管理し、「次へ」を押すたびに1コマずつ入れ替わる（前のコマは消え、新しいコマだけが表示される）
- そのページの最後のコマまで進んでから、次に「次へ」を押した時に初めて次のページ（新しい画像）に切り替わる。「前へ」も同様にコマ単位で戻り、ページの最初のコマで「前へ」を押すと前のページの最後のコマに戻る
- `audioJa` / `audioThai` はコマ単位ではなくページ単位のまま。コマを送っても音声は途切れない（アンマウントされない）ようにしている
- ページ送りの進捗表示は「1/3」のようなページ番号を主として維持し、コマが2つ以上あるページでは、その下に小さく「コマ 2/5」のような表示を追加する

### 5. 物語の一覧画面

- 複数の物語から選ぶ画面は今回不要。当面は1話固定で表示する

### 6. コレクション機能（2026-09-17実装）

- 本文中の単語タップ、およびReviewのフラッシュカードをめくると、その単語がコレクションとして`localStorage`に永続化される（`useCollectedWords`フック、キー: `taleglot:collectedWords`）
- コレクション画面（`Collection.jsx`）で、収集済みの単語を画像・タイ語・意味のカード形式で一覧表示する
- App.jsxのナビゲーションボタン（📚コレクション）から、StoryViewer/Reviewのどちらの画面からでもコレクション画面に遷移できる。「もどる」で元の画面に戻る
- デザインは最低限（design-system.mdのトークンを使った簡素なグリッド）で、凝った演出は無し

## Git運用ルール

`docs/GitHub運用ルール.md` を必ず参照すること。要点:
- mainブランチへの直接pushは禁止。作業用ブランチ(`feature/xxx`など)を作ってから作業する
- コミットメッセージは英語で「何をしたか」がわかる形にする
- **コミットする前に、コミットメッセージを提示して確認を取ること**
- push後はPull Requestを作成する

## 作業時の注意

- 指示された範囲を超えて実装を進めない(「ひな形だけ」と言われたら機能実装はしない、など)
- 変更内容が指示の範囲を超えそうな場合は、先に確認を取ること

## 作業完了時の確認ルール

見た目に影響する変更（CSS、コンポーネントの表示、レイアウトなど）を実装したら、コミットする前に必ず以下を行うこと：

1. `npm run dev` でローカルサーバーを起動する
2. 起動したURL（例: `http://localhost:5173`）を、変更内容の報告と一緒に必ず明記する
3. サーバーが起動しっぱなしで良いか、それとも確認後に停止すべきかは、ユーザーの指示を待つ（自動で停止しない）
4. ブラウザ自動化ツールでの目視確認だけで済ませず、ユーザー自身が同じURLを開いて確認できる状態を必ず残しておくこと

## 既知の課題（未修正・今後対応）

- `useAudioPlayer`の`toggle()`は多重発火防止のガードが無く、再生ボタンを素早く連続クリックすると`audio.play()`が二重に呼ばれ、実際は再生できているのにエラー表示（グレーアウト）になることがある(`src/hooks/useAudioPlayer.js`)
- `useAudioPlayer`の`handleEnded`/`handleError`（`<audio>`要素のネイティブonEnded/onErrorハンドラ）には、`toggle()`のplay()Promiseハンドラに追加したisStillCurrent相当のstale判定ガードが無い。`pause()`は仕様上ネットワーク取得を必ず中断するとは限らないため、要素がアンマウントされた後に遅延して`error`イベントが発火すると、無関係な現在の状態を上書きする可能性が理論上ある（未検証・低優先度）(`src/hooks/useAudioPlayer.js`)
- `useAudioPlayer`のstale-promiseガード（`isStillCurrent`）は要素とsrcの2値一致という組み合わせ判定であり、単調増加のジェネレーショントークンではない。現状は`pause()`/src差し替えが古いPromiseをほぼ即座に解決させるため実害はないが、将来`<audio>`要素のプーリングや同一src値の連続除去など質的に異なる変更を加える場合は、この前提が崩れて同種のバグが再発しうる点に注意(`src/hooks/useAudioPlayer.js`)
