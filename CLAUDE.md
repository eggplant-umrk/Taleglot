# CLAUDE.md

このファイルは、Claude Codeがこのリポジトリで作業する際の前提知識です。作業を始める前に必ず読んでください。

## プロジェクト概要

プロダクト名は **Taleglot**。タイの実在する民話を絵本形式で読みながらタイ語を学べる、Web学習アプリ。

## ターゲット層

小学校高学年。

## コンテンツ方針

- 創作しない。実在するタイの民話を出典付きで使う
- 民話の内容が小学高学年に重すぎる場合(暴力描写など)は、物語の核(テーマ・教訓)を保ったままマイルドに調整してよい

## 技術スタック

- React + Vite
- Next.jsは使わない(理由: チームメンバーの経験値がまだ浅く、SSRやルーティングを含むNext.jsより学習コストの低いVite+Reactを選択したため)

## ディレクトリ構成のルール

- 物語データ: `src/stories/storyXX.json`
- 物語データのスキーマ: README.md「物語データの追加方法」を参照
- 画像・音声素材: `public/stories/storyXX/`
- コンポーネント: `src/components/`

## 機能仕様（2026-09-06決定）

物語データのスキーマはREADME.md「物語データの追加方法」を参照。ここでは機能面の仕様をまとめる。

### 1. 本文中の単語タップ機能

- 本文中の重要な単語だけを太字にする（全部ではない）
- 太字部分をタップすると、詳細カードが開く
- 詳細カードの中身は「暮らし」「ことば」「価値観」の3カテゴリで固定（該当情報がないカテゴリは省略可）
- カードの閉じ方（×ボタン／外側タップ／両方）は未定。実装して触ってから決めるので、どちらにも対応できる作りにしておく

### 2. 日本語・タイ語の表示/音声トグル

- 日本語・タイ語、それぞれ独立した表示オン/オフ（目のマークのイメージ）。両方オンで併記も可能
- 日本語・タイ語、それぞれ独立した音声再生/停止ボタン（表示がオンの言語について再生できる）
- ページ送りは読み上げ中でも自由に進められる（読み上げを待つ必要はない）

### 3. 振り返り機能

- クイズ形式ではなく、フラッシュカード形式にする
- 表面: 絵、裏面: 単語・意味・読み方
- 絵本を最後まで読み終えたら、自動的に振り返り（フラッシュカード）画面に遷移する（ボタンを押す必要はない）

### 4. ページ送りのアニメーション

- アニメーションなし、一瞬で切り替わる形でよい（凝った演出は不要）

### 7. 本文のコマ送り（2026-09-16決定）

- 1ページの本文は「コマ」（1文単位）の配列（`pages[].text` / `pages[].thai`）で管理し、「次へ」を押すたびに1コマずつ入れ替わる（前のコマは消え、新しいコマだけが表示される）
- そのページの最後のコマまで進んでから、次に「次へ」を押した時に初めて次のページ（新しい画像）に切り替わる。「前へ」も同様にコマ単位で戻り、ページの最初のコマで「前へ」を押すと前のページの最後のコマに戻る
- `audioJa` / `audioThai` はコマ単位ではなくページ単位のまま。コマを送っても音声は途切れない（アンマウントされない）ようにしている
- ページ送りの進捗表示は「コマ 2/5」のようなコマ番号のみを表示する（Part選択機能導入により1Part=1ページとなり、ページ番号とコマ番号が実質同じ概念になったための整理。詳細は「既知の課題」参照）

### 5. 物語の一覧画面

- 複数の物語から選ぶ画面は今回不要。当面は1話固定で表示する

### 6. コレクション機能（2026-09-17実装）

- 本文中の単語タップ、およびReviewのフラッシュカードをめくると、その単語がコレクションとして`localStorage`に永続化される（`useCollectedWords`フック、キー: `taleglot:collectedWords`）
- コレクション画面（`Collection.jsx`）で、収集済みの単語を画像・タイ語・意味のカード形式で一覧表示する
- App.jsxのナビゲーションボタン（📚コレクション）はHome画面にのみ表示し、そこからコレクション画面に遷移できる（StoryViewer・Reviewでは画像を主役にするため非表示。2026-09-17変更）。「もどる」で元の画面に戻る
- デザインは最低限（design-system.mdのトークンを使った簡素なグリッド）で、凝った演出は無し

### 8. StoryViewerのオーバーレイレイアウト（2026-09-17変更）

- 画像は`.appの余白・ナビの高さを差し引いた残り全部の高さ`（`clamp(280px, calc(100vh - 224px), 760px)`。デスクトップ版は`.app`のpaddingが減る分`calc(100vh - 208px)`）でできるだけ大きく表示する。vhの大まかな割合ではなく既知の余白を実測して差し引く方式にすることで、画像とナビの間に余分な空白が残らないようにしている
- 本文（トグル・本文・音声ボタン）は画像下部に重ねる半透明の帯（`story-viewer__overlay-bar`）として表示する。帯は`top`を指定せず`bottom: 0`のみでcontentに合わせて高さが決まる（shrink-to-fit）ため、1コマ=1文の本文量に対して必要最小限の薄さに保たれる。想定を超えて長くなった場合の保険として`max-height: 45%`と帯内`overflow-y: auto`を残している
- 将来的に画像内の要素をタップしてコレクションする機能を追加する予定があるため、帯は画像上部を隠しすぎない薄さを保つ
- ページ送りナビ（前へ/次へ・進捗表示）は画像の外、帯のさらに下に配置する

## Git運用ルール

`docs/GitHub運用ルール.md` を必ず参照すること。要点:
- mainブランチへの直接pushは禁止。作業用ブランチ(`feature/xxx`など)を作ってから作業する
- コミットメッセージは英語で「何をしたか」がわかる形にする
- **コミットする前に、コミットメッセージを提示して確認を取ること**
- push後はPull Requestを作成する

## 作業時の注意

- 指示された範囲を超えて実装を進めない(「ひな形だけ」と言われたら機能実装はしない、など)
- 変更内容が指示の範囲を超えそうな場合は、先に確認を取ること

## 作業完了時の確認ルール

見た目に影響する変更（CSS、コンポーネントの表示、レイアウトなど）を実装したら、コミットする前に必ず以下を行うこと：

1. `npm run dev` でローカルサーバーを起動する
2. 起動したURL（例: `http://localhost:5173`）を、変更内容の報告と一緒に必ず明記する
3. サーバーが起動しっぱなしで良いか、それとも確認後に停止すべきかは、ユーザーの指示を待つ（自動で停止しない）
4. ブラウザ自動化ツールでの目視確認だけで済ませず、ユーザー自身が同じURLを開いて確認できる状態を必ず残しておくこと

## 既知の課題（未修正・今後対応）

- `useAudioPlayer`の`toggle()`は多重発火防止のガードが無く、再生ボタンを素早く連続クリックすると`audio.play()`が二重に呼ばれ、実際は再生できているのにエラー表示（グレーアウト）になることがある(`src/hooks/useAudioPlayer.js`)
- `useAudioPlayer`の`handleEnded`/`handleError`（`<audio>`要素のネイティブonEnded/onErrorハンドラ）には、`toggle()`のplay()Promiseハンドラに追加したisStillCurrent相当のstale判定ガードが無い。`pause()`は仕様上ネットワーク取得を必ず中断するとは限らないため、要素がアンマウントされた後に遅延して`error`イベントが発火すると、無関係な現在の状態を上書きする可能性が理論上ある（未検証・低優先度）(`src/hooks/useAudioPlayer.js`)
- `useAudioPlayer`のstale-promiseガード（`isStillCurrent`）は要素とsrcの2値一致という組み合わせ判定であり、単調増加のジェネレーショントークンではない。現状は`pause()`/src差し替えが古いPromiseをほぼ即座に解決させるため実害はないが、将来`<audio>`要素のプーリングや同一src値の連続除去など質的に異なる変更を加える場合は、この前提が崩れて同種のバグが再発しうる点に注意(`src/hooks/useAudioPlayer.js`)
- Part選択機能導入（PR #7）に伴う進捗表示の見直し: `StoryViewer.jsx`の進捗表示は現在「コマ X/Y」のみで、旧CLAUDE.mdの「ページ番号を主表示」というルール（本ファイル内「7. 本文のコマ送り」節）とは形式が異なる。ただし`buildParts`（`src/utils/parts.js`）が1ページ=1Partとして`pages`配列を1要素に絞り込む設計になったため、Part内では「ページ」と「コマ」が実質同じ概念になっており、現状の表示はこの新構造に対する妥当な適応と考えられる。あわせて`StoryViewer.jsx`には元々の複数ページ運用を前提にした`pageIndex`/`isLastPage`/`handlePrev`のページ跨ぎ分岐など、現在は`pages.length`が常に1のため到達しない死んだ分岐が残っている。将来的にStoryViewerを1Part=1ページの前提に合わせて整理するか、複数ページのPart運用を再び許容するかを検討する余地がある(`src/components/StoryViewer.jsx`, `src/utils/parts.js`)
- `Review.jsx`は`story.review`が空配列になるケース（`cards.length === 0`）で「振り返りカードがありません。」という行き止まり画面を表示し、`onFinish`を呼ばないため`markPartCompleted`が呼ばれずPartが「読了ずみ」にならない。`buildParts`はPartごとに、そのページの本文中に実際に登場する`annotationId`とマッチする`review[]`エントリだけに絞り込むため、今後のコンテンツ追加で「単語タップのマーカーを付けたのに対応する`review[]`エントリを用意し忘れたページ」を作ると発生しうる。現在のstory01.jsonでは全Partに2件以上のreviewカードがあるため未発生(`src/components/Review.jsx`, `src/utils/parts.js`)
- `src/utils/parts.js`の`partId`は`part${index + 1}`という配列インデックスから機械的に生成されており、`useCompletedParts`の永続化（localStorage `taleglot:completedParts`）もこの`partId`をキーにしている。将来`story01.json`の`pages`配列内でページの並び替えや先頭への挿入を行うと、既存ユーザーの「読了ずみ」記録が実際には読んでいない別のPartに紐付いてしまう(`src/utils/parts.js`, `src/hooks/useCompletedParts.js`)
- `src/utils/parts.js`のアノテーションID抽出用正規表現（`ANNOTATION_ID_PATTERN`）は、`StoryViewer.jsx`が単語タップ表示に使う`ANNOTATION_MARKER`の記法（`{{id:表示テキスト}}`）を独自に再実装したものであり、2箇所が二重管理になっている。将来この記法を変更する際に片方の更新を忘れると、タップ表示とReviewの絞り込みが静かに食い違いを起こす(`src/utils/parts.js`, `src/components/StoryViewer.jsx`)
- `docs/sasie/page01.jpg`〜`page15.jpg`と`public/stories/story01/page01.jpg`〜`page15.jpg`が同一内容の重複ファイルとして両方コミットされている。アプリコードから参照されているのは`public/stories/story01/`側のみで、`docs/sasie/`側はどこからも参照されていない（約3.7MB分の重複）。将来イラストを差し替える際に片方だけ更新すると2箇所が食い違う可能性がある
